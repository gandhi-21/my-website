import React, { Component } from 'react';
import data from '../../data/data.json';

class Home extends Component {
    render() {
        return(
            <div>Home screen</div>
        )
    }
}

export default Home;